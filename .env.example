# Defaults pra rodar AoP. Em container, todas as variaveis sao
# sobrescritas pelo docker-compose.yml do TV30 (service `aop`).

PORT=8080

# Hostname do broker MQTT. Em container = nome do service ('mosquitto').
# Fora de container = localhost (broker rodando no host).
MQTT_HOST=localhost

# Porta do host onde o Mosquitto expoe WebSocket. Default = 9001 (Linux puro).
# Em Windows+WSL2 mirrored com 9001/9002 ocupadas no host, definir 9003 no
# .env do TV30 (raiz) — esse valor eh propagado pro container e renderizado
# no HTML para o browser conectar.
MQTT_WS_PORT=9001

# Diretorio com userData.json + thumbs/. Em container = /user-files
# (volume montado de ./user-files na raiz do TV30).
USER_DATA_PATH=/user-files
